    <ul>
        {% for project in projects %}
            <li>
                <a href="{{ path('app_project_project', {'name': project}) }}">
                    {{ project }}
                </a>
            </li>
        {% endfor %}
    </ul>
